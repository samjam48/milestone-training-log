# ── Backend ──────────────────────────────────────────────────────────────────
DATABASE_URL=sqlite:///./data/milestone.db
APP_VERSION=0.1.0
BACKEND_PORT=8084
DEFAULT_USER_NAME=Sam
# Enable dev-only endpoints (POST /api/dev/reset). Never true in production.
APP_DEV_MODE=false

# ── Frontend ─────────────────────────────────────────────────────────────────
FRONTEND_PORT=5151
# Show the "Reset mock data" button in Settings. Never true in production.
VITE_DEV_MODE=false
